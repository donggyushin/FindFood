import React from 'react';
import {StyleSheet, View, Text} from 'react-native';

const LocalLoginScreen = () => {
  return (
    <View>
      <Text>Local login screen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});

export default LocalLoginScreen;

const loginRoutes = {
  home: 'HOME',
  local: 'LOCAL',
  newAccount: 'NEW',
};

const foodsRoutes = {
  foods: 'FOODS',
};

const mainDrawer = {
  foods: 'FOODS_DRAWER',
  logout: 'LOGOUT',
};

const rootRoutes = {
  loginRoutes,
  foodsRoutes,
  mainDrawer,
};

export default rootRoutes;
